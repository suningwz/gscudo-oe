<odoo>
    <data>
      <!-- explicit list view definition -->
  <!--
      <record model="ir.ui.view" id="gscudo-training.list">
        <field name="name">gscudo-training list</field>
        <field name="model">gscudo-training.gscudo-training</field>
        <field name="arch" type="xml">
          <tree>
            <field name="name"/>
            <field name="value"/>
            <field name="value2"/>
          </tree>
        </field>
      </record>
  -->
  
      <!-- actions opening views on models -->
  
      <record model="ir.actions.act_window" id="gstraining_planner_action_window">
        <field name="name">Pianificatore</field>
        <field name="res_model">gscudo-training.gstraining_planner</field>
        <field name="view_mode">tree,form,calendar</field>
      </record>
 
  
      <!-- server action to the one above -->
  <!--
      <record model="ir.actions.server" id="gscudo-training.action_server">
        <field name="name">gscudo-training server</field>
        <field name="model_id" ref="model_gscudo-training_gscudo-training"/>
        <field name="state">code</field>
        <field name="code">
          action = {
            "type": "ir.actions.act_window",
            "view_mode": "tree,form",
            "res_model": model._name,
          }
        </field>
      </record>
  -->
  
      <!-- actions -->

      <menuitem name="List" id="gscudo-training.menu_1_list" parent="gscudo-training.gstraining_menu_root"
                action="gscudo-training.gstraining_planner_action_window"/>

     </data>
  </odoo>